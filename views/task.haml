%h1
  =@task.tname.capitalize
%h3
  Description
=@task.tdisc
%h3
  Location
=@task.location
%h3
  Created At
=@task.created_at.strftime("%B %d, %Y at %I:%M %P")
%h3
  Shared with
-if !@task.approved.nil?
  =User.find(@task.friend_id).full_name
-if !@task.completed_at.nil?
  %h3 
    Completed At
  =@task.completed_at.strftime("%B %d, %Y at %I:%M %P")
